# 公网账号与在线对战 TODO

本文档独立规划“账号体系、数据库、用户数据隔离和公网在线对战”。现有 `TODO.md` 继续维护本地玩法、研究训练和局域网产品能力；本路线图不把尚未完成的平台能力提前包装成已可公网使用的功能。

## 目标

将当前单机/局域网应用逐步升级为具备以下能力的公网产品：

- 用户拥有稳定账号，可跨设备登录、退出和恢复会话。
- 私有数据以用户为归属单位隔离，任一接口都不能通过修改 ID 访问其他用户数据。
- 在线对局以服务端为权威，参与者身份、席位、走子、聊天和结果可持久化并可追溯。
- 普通象棋、揭棋和五子棋继续保持规则、状态、协议和隐私边界隔离。
- 服务重启、网络抖动和客户端刷新不会破坏正在进行的对局或造成身份错位。
- 具备公网运行所需的安全、限流、监控、备份、恢复和发布回滚能力。
- 本地历史数据可由用户明确导入当前账号；账号切换时不会串读上一个用户的浏览器数据。

## 优先级与工作量

- `P0`：公网版本的安全或数据正确性门禁；缺失时不得上线。
- `P1`：形成完整账号化产品闭环；P0 稳定后按批次实施。
- `P2`：竞争、社交、运营和横向扩容能力；不阻塞首个公网版本。
- `S`：约 0.5～1 人日。
- `M`：约 2～4 人日。
- `L`：约 5～10 人日，必须继续拆分。
- `XL`：跨模块平台改造，不按单个 PR 实施。

整体粗估：

| 目标版本      | 范围                                                         |  预计工作量 |
| ------------- | ------------------------------------------------------------ | ----------: |
| 账号在线 MVP  | 账号、数据库、休闲匹配、在线对局、对局历史、公网基础防护     | 22～30 人日 |
| 完整账号化 v1 | MVP + 研究/训练/残局/揭棋记录/五子棋历史等全部个人数据云端化 | 40～60 人日 |
| 竞争与社区 v1 | 完整账号化 + 棋钟、等级分、排行榜、举报管理和基础反作弊      | 55～75 人日 |

以上按一名熟悉当前代码的全栈开发估算，不包含第三方短信/邮件/云资源审核等待时间，也不等同于自然日排期。

## 首版范围与明确边界

### 首版必须包含

- [ ] 注册、登录、退出、会话续期和账号恢复闭环。
- [ ] MySQL 持久化、版本化迁移、事务、备份和恢复演练。
- [ ] 登录用户创建、邀请、匹配、落座、重连、完成和查看历史对局。
- [ ] 私有资源按 `owner_user_id` 隔离，共享对局按参与关系授权。
- [ ] HTTP 与 WebSocket 使用同一可信用户身份，不接受客户端自报 `userId`。
- [ ] 浏览器缓存按用户命名空间隔离，退出和切换账号后不得串数据。
- [ ] 揭棋继续维持公开、红方私有、黑方私有和裁判完整四种权限边界。
- [ ] 公网同源校验、限流、资源配额、日志、监控、健康检查和安全响应头。
- [ ] 数据导出、账号删除、共享对局保留/匿名化规则有明确产品决策。

### 首版默认决策

- [x] 对局继续采用“一次房间对应一盘对局”的模型；再来一局创建新的 `match_id`。
- [x] 公网快速匹配、执棋和实时观战必须使用已验证账号；匿名用户保留本地模式和公开只读回放。
- [x] 内部用户主键使用不可猜测 ID；登录标识、公开昵称和内部主键相互分离。
- [x] 复杂棋局/研究载荷优先保留现有 schema，以 `JSON + schema_version + revision` 存储。
- [x] 用户、会话、参与关系、邀请、聊天和可查询摘要使用关系表及数据库约束。
- [x] 首版使用单区域、单应用实例和托管 MySQL；不为假设中的规模提前引入 Redis。
- [x] 数据库基线固定为 MySQL 8.0.16+；不兼容 MariaDB，也不提供 MySQL/PostgreSQL 运行时切换，以免双倍维护 migration、约束和集成测试。
- [x] 现有局域网入口和席位恢复 token 保持可用；公网账号入口使用独立身份策略。
- [x] 旧浏览器数据只允许用户主动预览并导入当前账号，不在首次登录时静默上传。
- [x] 现有服务端本地 JSON 默认不自动归属给任何账号；如需保留，单独评估一次性迁移。

### 首版暂不包含

- [ ] 好友、关注、私信、公会或战队。
- [ ] 付费、会员、道具和广告。
- [ ] 多区域容灾和无状态多实例扩容。
- [ ] 完整反作弊判罚系统或客户端可信执行环境。
- [ ] 在线协作编辑同一研究树。
- [ ] CBR/XQF 等新增棋谱格式兼容。
- [ ] 原生移动端 App 和推送通知。

## 核心数据边界

“所有数据按用户隔离”不能等价为“所有表只加一个 `user_id`”。数据必须先分类，再确定所有权和访问关系。

### 用户私有资源

以下资源默认只有所有者本人可以读写：

- 对局库中的本地/人机/AI 对局文档。
- 研究局面、变招树、节点分析、标注和备注。
- 错题训练队列、练习状态和复习记录。
- 自定义残局、残局收藏和最近使用局面。
- 揭棋本人席位记录和本人私有备份。
- 五子棋本地历史。
- 用户偏好、界面设置和可同步的引擎设置。
- 私有回放短链及其权限配置（P2）。

要求：

- [ ] 每条私有资源包含 `owner_user_id`、`schema_version`、`revision`、`created_at`、`updated_at`。
- [ ] 所有 Repository 方法显式接收服务端解析出的 actor，不提供无 actor 的全量 `get/list/update/delete`。
- [ ] 自助接口优先使用 `/api/me/...`，避免客户端传入目标用户 ID。
- [ ] 列表、详情、更新、删除、批量导出、导入去重和来源跳转全部执行同一所有权校验。
- [ ] 后台管理能力使用独立权限和审计链路，不能复用普通用户接口的隐藏参数。

### 共享对局资源

在线对局由多名用户共同参与，不能归属到单一用户：

- 对局元数据、玩法、规则、阶段和最终结果。
- 红黑参与者、发起人、观众策略和参与时间。
- 服务端权威走法/事件流和并发版本。
- 邀请、申请、准备、协商和掉线状态。
- 房间聊天、禁言、删除记录和举报引用。

要求：

- [ ] 通过 `match_participants(match_id, user_id, role, side)` 表达参与关系。
- [ ] 参与者私有视图由“当前用户 + 对局参与关系 + 棋类投影规则”生成。
- [ ] 删除账号时不直接级联删除另一方仍有权查看的对局历史。
- [ ] 共享历史的保留、昵称快照、账号删除后匿名化和管理员访问规则在编码前确定。
- [ ] 观战可见范围、结束后可见范围、私密对局和邀请对局分别建模，不依赖 UI 隐藏。

### 服务端机密数据

以下数据永不直接返回给普通客户端：

- 密码哈希、登录失败计数、会话 token 哈希和恢复凭据。
- 揭棋完整初始暗子身份和裁判完整事件。
- 限流、封禁、举报处理和安全审计内部字段。
- 第三方邮件/短信/API 密钥及数据库凭据。

要求：

- [ ] 投影函数使用字段白名单重新构造响应，禁止对象展开后删除敏感字段。
- [ ] 日志、错误栈、监控标签和数据库慢查询不得记录密码、原始会话 token 或揭棋裁判载荷。
- [ ] 生产备份与开发样例分离；生产数据不得直接复制到本地开发环境。

### 全局或公开数据

- 内置残局、开局名称目录、规则语料和静态资源属于版本化公共数据。
- 公共大厅只返回可公开摘要，不返回会话、邀请、席位恢复或私有裁判字段。
- 公共回放只包含普通棋谱或揭棋公开投影。

## 建议数据模型

具体字段在 P0 设计阶段冻结，以下实体作为建模起点：

### 身份与账号

- `users`：内部 ID、账号状态、创建/更新时间、删除时间。
- `user_profiles`：展示昵称、头像引用、地区/语言等非认证资料。
- `auth_identities`：登录类型、规范化登录标识、验证状态；与公开资料分离。
- `password_credentials`：仅在自建密码登录时存在，保存内存困难型哈希及升级信息。
- `sessions`：会话 token 哈希、用户、创建/过期/撤销时间、设备摘要。
- `account_recovery_tokens`：一次性、短时有效、只保存哈希。
- `security_events`：登录失败、凭据变更、会话撤销等安全审计。

### 在线对局

- `matches`：玩法、规则、阶段、状态、原因、revision、时间和可见性。
- `match_participants`：用户、角色、红黑席位、昵称快照、准备/离开/结果信息。
- `match_state`：权威局面或紧凑事件文档；揭棋裁判载荷仅服务端读取。
- `match_moves`：是否逐手关系化在性能测试后决定；首版允许权威事件 JSON 与摘要并存。
- `match_invites`：邀请 token 哈希、目标/权限、过期、使用和撤销时间。
- `match_chat_messages`：作者用户、昵称快照、序号、内容、删除/审核状态。
- `match_chat_settings`：全员禁言、房间策略和规则配置。
- `match_proposals`：悔棋、议和、换边等协商；仅需跨重启恢复的状态持久化。

### 用户内容

- `user_games`：本地/人机/AI 对局文档。
- `studies`：研究与变招树 JSON、revision、名称和检索摘要。
- `training_tasks`：训练题、来源引用、状态和练习统计。
- `custom_endgames`：自定义残局和标签。
- `endgame_favorites`：用户与内置/自定义残局的收藏关系。
- `recent_positions`：最近局面，设置明确数量上限和自动清理策略。
- `jieqi_seat_records`：仅保存当前用户有权持有的席位投影或其可验证来源引用。
- `gomoku_game_records`：五子棋本地历史。
- `user_settings`：可同步设置；设备特定性能参数应单独分类。
- `data_import_jobs`：本地数据导入的幂等键、摘要、进度和结果。

### 关键数据库约束

- [ ] 登录标识规范化后唯一，软删除和重新注册语义明确。
- [ ] 同一对局同一用户最多一个有效棋手参与关系。
- [ ] 同一对局红、黑各最多一个有效席位。
- [ ] 走子更新同时校验 `match_id + expected_revision + 当前走子方`。
- [ ] 邀请 token 只存哈希，并以数据库原子更新保证最多使用一次。
- [ ] 用户私有资源的客户端幂等键在用户范围内唯一。
- [ ] 所有外键的删除策略显式声明，禁止依赖数据库默认行为。
- [ ] 高频查询建立索引，并用真实查询形状验证，不只根据字段名猜测。

## 目标架构与演进原则

```text
浏览器
  ├─ HTTPS /api/auth/*        注册、登录、恢复、会话
  ├─ HTTPS /api/me/*          当前用户私有资源
  ├─ HTTPS /api/matches/*     大厅、邀请、历史摘要
  └─ WSS                      在线对局、聊天、引擎任务
              │
              ▼
Node.js / Express / ws
  ├─ ActorContext             HTTP 与 WebSocket 统一身份
  ├─ 领域规则                 复用现有象棋/揭棋/五子棋权威逻辑
  ├─ Repository 接口          不让领域层依赖文件或具体 ORM
  ├─ 资源配额与限流           用户、IP、会话、引擎任务多维限制
  └─ 投影与授权               私有资源、共享对局、揭棋视角
              │
              ▼
MySQL
  ├─ 关系表                   用户、会话、参与者、邀请、聊天
  └─ JSON 文档               变招树、紧凑棋局、复杂训练数据
```

演进原则：

- [ ] 保留 `RoomManager`/规则核心的串行化和校验语义，通过 Repository 与身份适配器迁移。
- [ ] 局域网和公网使用显式场景分发，避免公网认证逻辑破坏现有 LAN 恢复链接。
- [ ] 首版活跃 WebSocket、在线状态和短时命令去重仍可驻留单进程内存。
- [ ] 需要横向扩容时再引入 Redis、跨实例发布订阅、分布式锁和 WebSocket 路由策略。
- [ ] 数据库是持久事实来源；内存缓存丢失后必须能从数据库安全恢复，不得反向覆盖新数据。
- [ ] 任何跨模块迁移先写兼容读取和回滚策略，再切换写路径。

## 实施顺序总览

```text
P0 决策与威胁模型
  -> P0 数据库底座
    -> P0 账号与统一身份
      -> P0 公网在线对局
        -> P0 公网防护与可运维上线
          -> P1 全部个人数据云端化
            -> P1 账号化体验与迁移闭环
              -> P1 棋钟/等级分
                -> P2 社交、运营、反作弊与多实例
```

任何批次只有通过本批发布门禁后才能进入下一批；不得同时大改账号、房间状态机和所有本地存储。

## 第零批：产品决策、权限矩阵与威胁模型（P0 / 2～3 人日）

目标：在写数据库迁移前冻结最容易导致返工的产品和安全边界。

状态：已完成。设计基线见：

- [账号与会话设计](docs/online-account-session-design.md)
- [MySQL 数据模型](docs/online-platform-data-model.md)
- [HTTP/WS 权限矩阵](docs/online-platform-access-control.md)
- [威胁模型与部署基线](docs/online-platform-threat-deployment.md)

### 账号决策

- [x] 首版采用邮箱密码登录；邮箱验证与密码恢复使用一次性邮件链接，不同时引入手机号或第三方登录。
- [x] 验证邮箱后才能匹配、创建公网对局、实时观战和聊天；未验证账号只能使用本地能力和账号安全流程。
- [x] 公开昵称允许重复；内部身份永远不依赖昵称。
- [x] 未登录用户可使用本地模式和查看公开已结束回放，不能实时观战或参与公网互动。
- [x] 明确待验证、正常、受限、封禁、待删除和已删除账号状态及对应权限。
- [x] 明确找回账号、修改凭据、撤销会话、30 天删除恢复期和最终清理流程。

### 数据与隐私决策

- [x] 为每类数据明确所有者、读取者、写入者、默认保留期、删除方式和导出方式。
- [x] 共享对局默认保留 365 天；一方注销后保留另一方历史并将删除方参与关系去标识化。
- [x] 正常聊天正文默认保留 30 天；删除正文与举报证据分别处理，安全审计默认保留 180 天。
- [x] 揭棋始终保持公开、红方私有、黑方私有和裁判态边界；裁判态随共享对局最长保留 365 天。
- [x] 本地数据允许用户明确选择导入分析结果、账号偏好和最近局面；设备性能设置单独分类。
- [x] LAN 席位 token、邀请 token、聊天 clientId、临时命令 ID 和设备秘密永不上传云端。
- [x] 上线地区的数据保护、内容治理和未成年人要求另行取得适用规则意见，不在代码 TODO 中自行推断法律结论。

### 威胁模型

- [x] 列出账号接管、凭据填充、会话窃取、CSRF、XSS、IDOR、WebSocket 越权和重放攻击。
- [x] 列出快速匹配双占位、重复落子、过期 revision、断线重连抢席和邀请重复使用。
- [x] 列出聊天刷屏、敏感内容、超长载荷、恶意 JSON、批量创建房间和观战连接耗尽。
- [x] 列出 Pikafish/Rapfi CPU、线程、Hash、任务队列和进程数量耗尽风险。
- [x] 列出揭棋裁判载荷通过通用导出、日志、错误响应、缓存或管理员接口泄露的风险。
- [x] 输出权限矩阵和攻击面清单，作为后续测试用例来源。

### 本批交付物与门禁

- [x] 新增账号与会话设计文档。
- [x] 新增数据库 ER 图/表说明和删除策略。
- [x] 新增 HTTP/WS 权限矩阵。
- [x] 新增部署拓扑、秘密管理和备份恢复说明。
- [x] 四份决策记录已交叉评审；下一批可以据此添加 MySQL 和 Repository 依赖。

## 第一批：数据库与 Repository 底座（P0 / 3～5 人日）

目标：建立可测试、可迁移、可回滚的 MySQL 底座，暂不改变用户可见行为。

状态：代码底座和本机 MySQL 9.4 集成测试已完成；自动备份调度和隔离恢复演练仍待执行。实现与操作说明见 [MySQL 实现与运维](docs/mysql-operations.md)。

### 基础设施

- [x] 选择 MySQL 驱动和迁移工具，记录选择依据；避免业务层直接散落 SQL/ORM 调用。
- [x] 增加开发、测试、生产三套连接配置和严格的环境变量校验。
- [x] 增加连接池、查询超时、事务辅助和优雅关闭。
- [x] 建立只能前进的版本化 migration；禁止应用启动时静默修改未知生产 schema。
- [x] 测试使用独立 database，测试间可可靠清理且不接触开发数据。
- [x] 增加数据库就绪检查；区分进程存活、应用可服务和数据库可读写。
- [x] 明确时间统一使用 UTC 存储，客户端按时区显示。

### Repository 边界

- [x] 为账号、会话、对局和用户文档定义最小 Repository 接口。
- [x] 将数据库实体与 WebSocket/HTTP 响应 DTO 分离。
- [x] 所有写入使用事务和数据库约束兜底，不能只依赖进程内 Map。
- [x] 保留现有 JSON Repository 作为 LAN/迁移对照，不在首个 PR 中删除。
- [x] 对 JSON 文档继续执行现有 schema 校验，不因为进入数据库就信任载荷。
- [x] 为 revision 冲突、唯一约束、外键冲突和数据库不可用定义稳定错误类型。

### 备份与恢复

- [ ] 配置自动备份和保留策略，不把“云数据库有备份”当作恢复演练。
- [ ] 在隔离环境完成一次从备份恢复，并验证账号、对局和揭棋裁判态的一致性。
- [x] 记录 migration 前备份、失败回滚和应用版本兼容窗口。

### 本批测试与门禁

- [x] Repository 单元/集成测试覆盖创建、更新、冲突、删除、事务回滚和重连。
- [x] migration 在空库和上一版本 schema 上均可执行。
- [x] 数据库断开时接口返回可控错误，进程不写入半份状态。
- [x] `pnpm --filter server test`、服务端构建和 `git diff --check` 通过。

## 第二批：账号、会话与统一 ActorContext（P0 / 5～8 人日）

目标：让 HTTP 和 WebSocket 都能获得可信的当前用户，但暂不迁移全部业务数据。

### 注册与登录

- [x] 注册接口规范化登录标识，统一错误响应，避免泄露账号是否存在。
- [x] 自建密码时使用经过评审的内存困难型密码哈希，记录参数版本并支持以后升级。
- [x] 登录失败按 IP、账号标识和设备会话多维限流，成功后合理重置计数。
- [x] 会话 token 使用高熵随机值，数据库只保存哈希；支持过期、轮换和主动撤销。
- [x] 浏览器使用 `HttpOnly + Secure + SameSite` Cookie；明确 CSRF 防护策略。
- [x] 登录、注册、退出、查询当前用户、修改资料、修改凭据和撤销会话有稳定 API。
- [x] 找回凭据 token 一次性、短时有效，使用后撤销相关旧会话。
- [x] 邮件/短信发送失败可重试但不能重复创建无限有效凭据。

### HTTP 身份

- [x] 增加统一认证中间件，产出 `ActorContext`，业务代码不直接解析 Cookie/header。
- [ ] 区分匿名、已登录、受限、封禁和管理员 actor。
- [x] `/api/me/*` 不接受客户端覆盖当前用户 ID。
- [ ] 管理接口使用独立路由、权限和审计日志，不靠前端隐藏入口。

### WebSocket 身份

- [x] Upgrade 时执行来源校验和会话解析，连接对象绑定稳定 `userId/sessionId`。
- [ ] 会话过期、主动退出、封禁和撤销全部会话后，已有连接按策略关闭或降权。
- [x] 重连重新认证，不复用旧内存角色作为可信来源。
- [ ] HTTP 创建的邀请/对局与 WebSocket 订阅使用同一 actor。
- [ ] 消息处理不接受 `userId`、`role`、`isOwner` 等客户端自报权限字段。

### 前端账号骨架

- [x] 增加注册、登录、恢复、退出和会话失效页面/弹窗。
- [x] 增加当前用户上下文和启动时会话恢复，避免页面闪现上一个用户的数据。
- [x] 统一处理 401、403、429、会话过期和网络失败。
- [x] 登录表单错误就地展示，提交状态可见，移动端操作区域符合现有规范。

### 本批测试与门禁

- [x] 覆盖注册、重复标识、登录、退出、过期、撤销、恢复和密码升级。
- [x] 覆盖 Cookie 属性、CSRF、跨来源 Upgrade 和 WebSocket 会话撤销。
- [ ] 覆盖两个账号在同一浏览器依次登录时的 UI/缓存隔离。
- [x] 安全日志不含密码、验证码、恢复 token 或原始会话 token。
- [x] 客户端/服务端测试、全量构建和桌面/移动端账号流程验收通过。

## 第三批：账号化公网对局与匹配（P0 / 6～9 人日）

目标：在不破坏 LAN 模式的前提下，将公网房间身份从匿名 capability token 转为账号参与关系。

### 模型迁移

- [x] 公网 `match` 继续保持一局一 ID；内部是否继续使用 `Room` 命名不影响用户语义。
- [x] 创建对局时写入发起人参与关系，不再只保存 `ownerHash`。
- [x] 红黑席位绑定用户参与关系；显示昵称使用开局时快照，避免后续改名改变历史。
- [x] 邀请 token 只授予一次加入/申请权限，加入后长期身份来自账号参与关系。
- [x] 公网席位恢复由登录会话完成；跨设备无需复制长期席位凭据。
- [x] LAN 模式继续使用现有 owner/seat token，并与公网参与关系代码显式分发。

### 快速匹配

- [x] 匹配条件明确包含棋类、规则、休闲/排位、棋钟档位及可扩展约束。
- [x] 以数据库事务原子认领候选，防止并发请求加入同一空位。
- [x] 同一用户不能同时占用多个相同匹配队列位置。
- [x] 取消、超时、断线、重复点击和多标签页匹配请求保持幂等。
- [x] 无候选时创建等待对局；发起人断线后的清理与重排策略明确。
- [x] 首版单实例可以保留内存通知，但数据库中必须能识别匹配结果和参与关系。

### 权威对局与重连

- [x] 走子同时校验当前用户席位、轮次、对局阶段和 expected revision。
- [x] 命令 ID 在用户/对局范围去重，重复发送不得重复落子或重复结算。
- [x] 服务重启后从数据库恢复 waiting/playing 对局，并重建掉线宽限状态。
- [x] 掉线判负、双方离线、主动认输、议和和异常终止都生成稳定结果原因。
- [x] 同一用户新连接接管席位时，旧连接明确降权或关闭，行为与现有席位恢复一致。
- [x] 结束对局只读；重新打开显示历史，不重新加入活跃房间。
- [x] 再来一局创建新 ID，并以可选 `previous_match_id` 串联。

### 对局历史与可见性

- [x] `/api/me/matches` 只列出当前用户参与的对局，支持分页和棋类/结果/时间筛选。
- [x] 公共大厅、本人历史和公开回放使用不同 DTO。
- [x] 私密邀请对局不进入公开大厅或公共历史。
- [x] 观战权限在服务端校验；前端 URL 可猜测不等于有访问权。
- [x] 历史记录不暴露邀请 token、连接信息或不属于当前席位的揭棋私有事件。

### 聊天与身份

- [x] 聊天作者绑定 `user_id`，同时保存发送时昵称快照。
- [x] 限流由 IP + userId + matchId 共同约束，账号切换不能绕过全部限制。
- [x] 禁言目标从临时 `authorId` 迁移为参与用户或对局内匿名身份。
- [x] 删除消息保留必要的审核状态，不在广播或历史接口返回已删除正文。
- [x] 首版禁止未登录用户实时观战和聊天；以后如开放，必须有独立匿名身份、连接配额和更严格限流。

### 揭棋隐私门禁

- [x] 数据库仅保存一份权威裁判事件/状态，不持久化三份可能漂移的投影。
- [x] 每次读取根据当前 actor 的参与席位生成公开或席位投影。
- [x] 发起人但未落座、管理员常规查看和观众默认都不能获得席位私有信息。
- [x] 账号换绑、邀请、重连、服务重启和历史回放不改变原席位信息边界。
- [x] 通用对局导出、后台列表、日志和错误响应扫描不得出现完整暗子身份。

### 本批测试与门禁

- [x] 两个账号并发匹配只能形成一个确定的红黑组合。
- [x] 非参与账号不能走子、准备、议和、悔棋、读私密历史或冒充聊天作者。
- [x] 多标签页、换设备、断网重连、服务重启和过期命令全部覆盖。
- [x] 普通象棋、揭棋、五子棋共享房间壳但分别通过规则一致性测试。
- [x] 揭棋红、黑、观众、未落座房主四种身份逐手比较投影。
- [x] 现有 LAN 全流程回归通过，邀请和席位恢复链接行为不变。

## 第四批：公网安全、资源治理与可运维部署（P0 / 5～8 人日）

目标：让系统能够安全、可观察、可恢复地暴露在公网，而不仅是在服务器上“能跑起来”。

### 网络与 HTTP 安全

- [x] 所有生产流量使用 HTTPS/WSS，HTTP 只做安全重定向。
- [x] 所有模式都执行允许来源校验，不再只在 `LAN_MODE` 下检查 WebSocket Origin。
- [x] 配置可信反向代理范围，禁止任意客户端伪造原始 IP header。
- [x] 设置安全响应头、合理 CSP、内容类型保护和静态资源缓存策略。
- [x] API、WebSocket 和上传/导入分别设置负载大小、字段数量和嵌套深度上限。
- [x] CORS 默认同源；如确有跨域客户端，使用明确白名单和凭据策略。

### 限流与资源配额

- [x] 注册、登录、恢复、创建对局、快速匹配、聊天、邀请和公开查询分别限流。
- [x] 限流同时考虑 IP、用户、会话和资源 ID，不能仅依赖单个内存 Map。
- [x] 对单用户活跃房间数、并发连接数、观战数、聊天频率和历史查询设置上限。
- [x] 对 Pikafish/Rapfi 分析、提示、线程、Hash、队列长度和总进程数设置服务端硬上限。
- [x] 公网实战默认禁止客户端直接开启会泄露或消耗巨大的服务端分析能力。
- [x] 超限返回稳定错误和重试时间，不以进程崩溃或无限排队作为背压。

### 可观测性

- [x] 使用结构化日志并生成 requestId/connectionId/matchId；用户 ID 只在必要范围记录。
- [x] 记录注册登录成功率、匹配等待、活跃连接、对局完成、异常终止和数据库错误指标。
- [x] 记录引擎队列、搜索耗时、超时、进程退出、CPU/内存和任务拒绝指标。
- [x] 增加 readiness/liveness，不把引擎缺失、数据库故障和进程存活混成一个状态。
- [x] 定义最小告警：登录异常峰值、5xx、数据库连接耗尽、WS 断开激增、引擎资源超限、备份失败。
- [x] 日志采样和保留有上限，避免聊天正文、棋局载荷或敏感字段进入高基数标签。

### 部署与运行

- [x] 固化生产构建和启动方式，明确 Node、Pikafish、揭棋引擎、Rapfi 与权重版本。
- [x] 引擎二进制、数据库凭据和会话密钥通过部署系统准备，不进入镜像历史或仓库。
- [x] 优雅停机先停止接收新连接，再处理/终止引擎任务、持久化必要状态并关闭连接池。
- [x] 数据库 migration 与应用发布顺序支持至少一个版本的滚动兼容或明确维护窗口。
- [ ] 建立 staging 环境，使用独立账号、数据库、密钥和引擎配额。
- [x] 发布前后执行备份、migration、冒烟、指标观察和回滚检查表。

### 本批测试与门禁

- [ ] 完成基础并发/负载测试：登录、匹配、观战、聊天、走子和历史查询。
- [ ] 模拟数据库短暂中断、慢查询、磁盘/连接池压力和应用重启。
- [x] 模拟引擎不可用和任务洪峰，确认真人在线对局核心流程不被拖垮。
- [ ] 在 staging 完成一次发布、migration、回滚和备份恢复演练。
- [x] 未通过安全与恢复门禁前，产品文案继续使用“局域网对战”，不得切换为公网在线。

> 本地实现与自动化门禁已落地；基础写负载、数据库/磁盘故障注入、独立 staging 建设及真实发布/回滚/恢复演练必须在外部环境留存证据，因此对应项目保持未勾选。执行顺序与记录模板见 `docs/online-production-runbook.md`。

## 第五批：全部个人数据云端化（P1 / 8～12 人日）

目标：将当前浏览器/本机存储逐类迁移为账号私有云端资源，并保留明确的离线和冲突反馈。

### 通用同步协议

- [x] 定义统一文档元数据：ID、owner、schemaVersion、revision、createdAt、updatedAt、clientMutationId。
- [x] 创建、更新、删除和批量导入均支持幂等；网络重试不得产生重复记录。
- [x] 更新使用 expected revision，冲突时保留本地待保存快照并让用户选择处理。
- [x] 客户端缓存按 `userId + resourceType` 命名空间隔离。
- [x] 服务端响应注销/账号切换时，立即丢弃内存中的上一用户资源和未完成请求结果。
- [x] 本地模式在未登录时继续可用，但明确显示“仅保存在此设备”。
- [x] 登录后新建数据的默认保存位置、离线行为和失败降级由产品明确展示。

### 分资源迁移顺序

1. 对局库
   - [x] 将当前 `data/games` 能力改为当前用户私有 Repository。
   - [x] 单标签页编辑租约同时校验资源所有权；跨用户不能接管。
   - [x] 普通棋局导入导出继续排除揭棋裁判数据。

2. 研究与变招树
   - [x] 云端保存完整 variation tree、nodeId、分析、标注和来源引用。
   - [x] 自动保存使用 revision 和最新待保存快照，保持现有冲突/失败语义。
   - [x] 搜索摘要由服务端生成或校验，不能信任客户端伪造 owner。

3. 训练题
   - [x] 训练题归属当前用户，来源研究/对局必须同时具备访问权。
   - [x] 来源被删除后保留可独立训练的必要快照，并将链接标记为失效。
   - [x] 去重键在用户范围内生效，不跨用户合并。

4. 自定义残局与收藏
   - [x] 自定义残局私有；内置残局是全局版本化数据。
   - [x] 收藏关系绑定用户，内置目录升级后处理已移除条目。

5. 揭棋本人记录
   - [x] 只接受当前用户实际席位产生或合法导入的席位投影。
   - [x] 红方记录不可在修改 audience 后作为黑方记录导入。
   - [x] 普通研究、训练、FEN、分享接口继续拒绝读取该数据。

6. 五子棋历史、最近局面和设置
   - [x] 五子棋记录保持规则字段和合法重放校验。
   - [x] 最近局面设置数量上限、去重和自动清理。
   - [x] 区分账号偏好与设备性能设置；后者可按设备保存或只留本地。

### 数据导出与删除

- [x] 用户可导出其私有数据和有权获得的共享对局视图。
- [x] 导出异步生成时有过期时间、下载鉴权和审计，不产生永久公开链接。
- [x] 账号删除前展示影响范围并要求重新认证。
- [x] 私有资源删除、共享历史匿名化、会话撤销、恢复窗口和最终清理分阶段执行。
- [x] 删除任务幂等且可审计，失败能安全重试。

### 本批测试与门禁

- [x] 对每个资源执行 A 创建、B 列表/读取/更新/删除/导出/引用的完整越权矩阵。
- [x] 同一账号两台设备并发编辑覆盖正常、冲突、离线重试和删除冲突。
- [x] 同一浏览器 A 退出 B 登录后，不显示 A 的缓存、Toast、自动保存结果或最近记录。
- [x] 复杂研究 JSON、揭棋记录和五子棋记录继续执行现有语义校验。
- [x] 每迁移一种资源都保留定向测试，不等全部迁完后一次性补测试。

## 第六批：本地数据导入与账号化体验闭环（P1 / 4～7 人日）

目标：让老用户安全理解和控制“本机数据如何进入账号”，并补齐跨设备产品体验。

### 一次性本地导入

- [x] 登录后扫描旧全局 localStorage key，只生成分类和数量摘要，不立即上传。
- [x] 用户明确选择要导入的资源类型，可预览冲突和跳过敏感数据。
- [x] LAN 房间 token、邀请 token、聊天 clientId 和其他 capability 永不导入云端。
- [x] 导入请求携带稳定 importId；刷新、断线和重复提交保持幂等。
- [x] 冲突按资源定义处理：同 ID 同内容跳过、同 ID 不同内容复制或由用户选择。
- [x] 全部成功后才标记该账号/设备已导入；部分失败保留可重试明细。
- [x] 不自动删除旧本地数据，提供确认后的清理操作和导出备份提示。

### 账号中心

- [x] 展示资料、登录标识验证状态、活跃会话和最近安全事件。
- [x] 支持修改公开昵称、修改凭据、退出当前设备和撤销其他设备。
- [x] 展示云端数据分类、数量、占用/配额、导出和删除入口。
- [x] 展示本人在线对局历史并区分普通象棋、揭棋和五子棋。
- [x] 账号受限、封禁、待删除和恢复期有明确只读/不可用状态。

### 保存与离线体验

- [x] 统一显示本地、待同步、保存中、已同步、冲突、失败和只读状态。
- [x] 在线对局的权威状态不使用离线写队列；断线时禁止本地假落子。
- [x] 研究/训练等私有文档可保留最新待保存快照，但恢复连接后仍需 revision 校验。
- [x] 退出页面和切换账号前提示未保存数据，并提供重试或本地导出。
- [x] 移动端保持棋盘优先，账号和同步提示不覆盖核心对局操作。

### 本批测试与门禁

- [x] 覆盖无旧数据、单类数据、全部数据、损坏数据、容量超限和部分失败。
- [x] 覆盖重复导入、跨账号导入同一设备数据和导入后账号切换。
- [x] 桌面/移动端完整验收注册到导入、跨设备查看、编辑冲突、导出和注销。

## 第七批：发布验收、灰度与回滚（P1 / 4～6 人日）

目标：以可回退方式从局域网能力切换到首个公网账号化版本。

### 自动化发布门禁

- [x] 客户端和服务端全部测试、全量构建、格式检查和 `git diff --check` 固定执行。
- [x] 数据库 migration、Repository 集成、账号权限矩阵和并发匹配测试进入 CI。
- [x] 揭棋跨视角隐私、普通棋谱导入导出和三棋类规则一致性继续作为阻断测试。
- [x] 增加 API/WS 契约测试，协议字段变更同步更新客户端、服务端和文档。
- [x] 增加关键依赖和容器/运行镜像的漏洞检查策略。

### 灰度步骤

- [ ] staging 使用与生产同拓扑的小规模环境完成全流程验收。
- [ ] 首批只向受控测试账号开放，限制注册数、匹配队列和引擎配额。
- [ ] 观察注册、登录、匹配成功率、异常终局、数据库冲突、WS 重连和资源占用。
- [ ] 扩大开放前复核备份可恢复、告警有效、客服/问题反馈入口可用。
- [ ] 公网功能使用可关闭的服务端开关；关闭后不影响本地和 LAN 模式。

### 回滚策略

- [ ] 应用回滚不逆向破坏数据库；migration 遵守扩展、双读/兼容、切换、清理顺序。
- [ ] 协议在灰度窗口兼容前一客户端版本或强制安全刷新。
- [ ] 公网入口关闭时，进行中对局按明确策略完成、暂停或判定，不直接丢弃。
- [ ] 回滚演练记录恢复时间、数据损失窗口和人工步骤。

### 首个公网版本完成标准

- [ ] 两个新账号能注册、登录、匹配、完成三类对局并在各自历史中查看正确视图。
- [ ] 非参与者无法访问私密对局；参与者不能访问对手揭棋私有事件。
- [ ] 同账号换设备可重连；不同账号不能凭 URL 或旧缓存接管席位。
- [ ] 应用和数据库重启后对局及用户数据保持一致。
- [ ] 限流、引擎配额和异常隔离能承受约定的首发容量。
- [ ] 已完成备份恢复、migration 回滚、账号删除和数据导出演练。
- [ ] 达到门禁后，产品入口才从“局域网对战”新增独立“在线对战”，两者不混用。

## 第八批：棋钟、等级分与竞争对局（P1 / 8～12 人日）

目标：在休闲在线稳定后提供真正可比较的竞争对局；不与账号底座同期开发。

### 服务端权威棋钟

- [ ] 定义基础时间、每步加秒/延时、等待开始和断线期间计时规则。
- [ ] 服务端保存单调时钟基准和持久化快照，客户端只展示估算，不做权威扣时。
- [ ] 落子事务同时更新局面、轮次和剩余时间，超时与落子竞态只有一个结果。
- [ ] 重启恢复、网络延迟、后台标签页和设备时间错误不改变权威结果。
- [ ] 五子棋和象棋可共用计时壳，但终局和规则状态继续隔离。

### 等级分

- [ ] 明确采用的评分模型、初始分、暂定期、分池和赛季策略。
- [ ] 只对满足条件的排位对局结算；好友房、异常终止和管理员作废不计分。
- [ ] 对局结果与积分变更在同一事务中最多结算一次。
- [ ] 保存结算前后分数和原因，支持审计与必要的补偿事务。
- [ ] 普通象棋、揭棋、五子棋及不同规则是否分池在设计阶段确认。

### 匹配体验

- [ ] 按等待时间逐步扩展分差和其他条件，算法参数可配置并可观测。
- [ ] 防止重复对手刷分、同时多队列占位和取消匹配滥用。
- [ ] 排位对局中服务端分析/提示入口关闭，客户端明确提示公平竞赛规则。

### 本批门禁

- [ ] 棋钟边界、落子/超时竞态、重启恢复和长连接延迟有固定测试语料。
- [ ] 等级分重复消息、事务重试和作废补偿不会重复结算。
- [ ] 排行榜只读取结算记录，不从客户端提交的分数生成。

## 第九批：社交、内容治理与运营能力（P2）

### 社交

- [ ] 好友申请、接受、删除、拉黑和隐私设置。
- [ ] 好友邀请对局和在线状态共享边界。
- [ ] 通知中心及已读状态；首版不依赖原生推送。
- [ ] 用户主页公开字段与本人私有资料分离。

### 举报与管理

- [ ] 举报对局、聊天和用户，保存必要证据快照而不是任意复制全部私有数据。
- [ ] 管理员角色、最小权限、二次认证和完整审计。
- [ ] 禁言、临时限制、封禁、解封和申诉状态机。
- [ ] 内容删除、证据保留和用户可见状态按适用规则评审。
- [ ] 管理后台与普通 API 分离部署或至少分离路由、权限和日志。

### 基础反作弊

- [ ] 明确休闲与排位的公平竞赛规则和可执行处罚范围。
- [ ] 收集服务端已有对局时序、切屏/断线等低侵入证据前，先完成隐私评审。
- [ ] 引擎相似度只能作为风险信号，不能单独自动判罚。
- [ ] 申诉和人工复核链路在自动限制前完成。
- [ ] 不宣称能阻止用户使用外部设备或外部引擎。

## 第十批：横向扩容与高可用（P2）

只有单实例指标证明需要扩容时才启动。

- [ ] 将在线状态、短时命令去重、匹配通知和房间广播从单进程内存抽象出来。
- [ ] 引入 Redis 或等价组件承担 presence、Pub/Sub、分布式限流和必要锁；数据库仍是持久事实来源。
- [ ] 快速匹配使用数据库事务或专用队列，Redis 锁失效不能导致双占位。
- [ ] 明确 WebSocket 粘性会话或房间分片策略，以及实例退出时的迁移行为。
- [ ] 将 Pikafish/Rapfi 任务放入受配额控制的独立 worker 池，避免 Web 进程被计算任务拖垮。
- [ ] 多实例下对局命令保持全局顺序和幂等，revision 冲突可恢复。
- [ ] 进行故障注入：实例崩溃、Redis 短断、数据库主从切换和消息重复/乱序。
- [ ] 指标证明单实例瓶颈已解除后才扩大生产副本数。

## 后续云端能力（P2）

- [ ] 服务端短链：所有者、公开/私密/指定用户权限、过期、撤销和访问限流。
- [ ] 云端分享封面与头像：对象存储、类型校验、尺寸限制、内容治理和过期清理。
- [ ] 在线协作研究：成员权限、操作日志、并发编辑模型、冲突和回滚；不复用实时对局状态机。
- [ ] 多设备离线编辑：仅在现有 revision 冲突不足以满足需求时评估操作日志/CRDT。
- [ ] 数据分析：优先使用脱敏聚合，建立采集目的、保留期和用户控制入口。

## 跨批次测试矩阵

### 账号隔离

- [ ] A 创建私有资源，B 对列表、详情、更新、删除、导出、复制、来源跳转均得到拒绝或不存在。
- [ ] A 退出后 B 在同一浏览器登录，不出现 A 的缓存、搜索结果、自动保存回调或错误提示。
- [ ] 修改 URL、body、query、WebSocket message 中的资源 ID 不能绕过 actor 权限。
- [ ] 被删除、受限、封禁和会话撤销账号的 HTTP/WS 行为一致。

### 共享对局

- [ ] 红黑双方只获得自己的参与权限，观众只能执行允许的只读操作。
- [ ] 邀请最多使用一次，过期、撤销和错误目标均拒绝。
- [ ] 两个并发落子、重复命令和过期 revision 不会生成分叉权威状态。
- [ ] 服务重启后席位、阶段、走法、聊天序号和终局状态一致。

### 揭棋隐私

- [ ] 红、黑、观众和裁判对同一事件逐帧比较可见身份。
- [ ] 双方暗吃、重连、换设备、改昵称、账号删除和历史回放不改变原授权。
- [ ] 公共 DTO、日志、导出、短链、数据库管理列表和错误响应执行敏感字段扫描。
- [ ] 普通棋谱、研究、训练和分享路径不能读取揭棋席位/裁判载荷。

### 可靠性与安全

- [ ] 数据库错误、超时、重试和事务回滚不产生半完成账号、参与关系或积分。
- [ ] 应用重启、优雅停机、非优雅崩溃和网络分区均有明确恢复结果。
- [ ] 超长、畸形、深嵌套和高频载荷被限制且不会阻塞事件循环。
- [ ] 登录、匹配、聊天和引擎任务洪峰触发限流而不是耗尽资源。

## 每批固定完成检查

- [ ] 变更仅落在本批范围，现有脏工作树和无关文件保持不变。
- [ ] 数据模型、迁移、HTTP/WS 契约和权限矩阵同步更新文档。
- [ ] 存储、协议、权限或状态流变更包含对应定向测试。
- [ ] `pnpm --filter client test` 通过。
- [ ] `pnpm --filter server test` 通过。
- [ ] `pnpm build` 通过。
- [ ] `pnpm format:check` 通过。
- [ ] `git diff --check` 通过。
- [ ] 桌面端和移动端主流程验收通过，浏览器控制台无新增错误。
- [ ] 揭棋隐私、三棋类规则一致性和 LAN 回归门禁通过。
- [ ] 数据库 migration、备份/恢复和回滚步骤在对应批次实际演练。

## 风险与排期增量

以下选择会明显增加首版工作量，应在第零批确认而不是开发中途加入：

| 增量范围                     | 额外工作量参考 | 主要原因                                    |
| ---------------------------- | -------------: | ------------------------------------------- |
| 每增加一种第三方登录         |      2～4 人日 | 回调、账号绑定、冲突、撤销和供应商环境      |
| 手机验证码登录               |      2～4 人日 | 供应商、风控、频控、成本和交付失败          |
| 保留并归属旧服务端 JSON 数据 |      2～4 人日 | 身份映射、校验、幂等、回滚和坏数据处理      |
| 完整本地数据导入             |      3～5 人日 | 多 schema、冲突、损坏数据和用户确认         |
| 服务端权威棋钟               |      4～6 人日 | 时间竞态、重启恢复、延迟和终局事务          |
| 等级分和排行榜               |      4～7 人日 | 分池、幂等结算、作废、赛季和审计            |
| 举报与最小管理后台           |     5～10 人日 | 权限、证据、审计、操作状态和安全            |
| 基础反作弊                   |   10～20+ 人日 | 证据质量、误判、复核、隐私和持续运营        |
| 多实例和 Redis               |     8～15 人日 | presence、Pub/Sub、锁、顺序、路由和故障恢复 |

## 推荐启动范围

当前建议只启动以下内容：

1. 第零批：完成账号决策、数据分类、ER 图、权限矩阵、威胁模型和部署基线。
2. 第一批：引入 MySQL 和 Repository 接口，但不改变现有用户流程。
3. 第二批：完成账号、会话、HTTP/WS 统一身份和最小前端账号骨架。
4. 第三批：只迁移公网休闲对局、匹配和本人对局历史，保留现有 LAN 模式。
5. 第四批：完成公网安全、资源配额、监控、备份和 staging 发布门禁。

上述五批稳定后，再按“对局库 → 研究 → 训练 → 残局/收藏 → 揭棋记录 → 五子棋历史/设置”的顺序推进全部个人数据云端化。棋钟、等级分、社交和多实例均不得反向阻塞账号在线 MVP。

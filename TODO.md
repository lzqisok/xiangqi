# 待办清单

本文档按当前 `main` 分支功能现状维护。目标不是罗列所有想法，而是把「现在最值得做、做完能明显提升可用性」的任务排清楚。

优先级含义：

- `P0`：当前最实用、建议下一批优先做。
- `P1`：价值明确，但需要先设计数据模型或与 P0 打配合。
- `P2`：长期产品化方向，投入较大，暂不作为近期主线。

难度：

- `S`：小改动，通常 0.5 天内。
- `M`：中等功能，通常 1-2 天。
- `L`：较大功能或跨模块改造，需要拆阶段。

## 产品体验重构路线图

目标：把当前按技术能力组织的多层入口，调整为围绕用户目标的统一流程：

```text
棋局空间 -> 棋类控制台 -> 开始一局 / 和朋友下 / 训练与研究
                         -> 对局准备 -> 正在对局 -> 结果 / 复盘
                                      -> 局域网等待房间
```

### 第一批：导航、模式模型与本地工作台（已完成）

- [x] 首页突出“继续上次对局”，棋类入口说明各自完整能力，不再只要求用户逐层选择分类。
- [x] 象棋和五子棋控制台统一为“开始一局 / 和朋友下 / 训练与研究”三个目标入口。
- [x] 将象棋的“对手类型、规则、训练内容”从六个并列模式拆开：人机/双人/AI 对决属于对手，普通象棋/揭棋属于规则，残局/研究属于训练内容。
- [x] 保留现有 URL、存档数据、`GameMode`、引擎与规则实现，产品调整不触发历史数据迁移。
- [x] 象棋工作台由固定“棋谱 + 棋盘 + 工具”三列改为“棋盘 + 阶段面板”两列，棋谱进入工具标签。
- [x] 标签随阶段出现：进行中显示对局、棋谱、分析；结束后再突出结果、复盘和变招；研究模式保留变招入口。
- [x] 1280px 桌面宽度下不出现页面级横向滚动；移动端维持棋盘优先、工具纵向展开。

### 第二批：操作层级与统一反馈

- [x] 每个阶段只保留一个视觉主操作：开始、准备、自动播放、再来一局或开始复盘。
- [x] 将悔棋、提示、下一步等放入二级操作，将翻转、FEN、图片导出和另存研究归入更多工具。
- [x] 新对局入口固定在工作台顶部，并在离开前明确显示保存状态。
- [x] 统一连接、重连、只读、保存冲突、AI 思考和异步提交状态；持续问题使用状态条，短暂成功使用 Toast，字段错误就地展示。
- [x] 以产品内弹窗替换 `alert`、`prompt`、`confirm`，危险操作集中到独立区域。
- [x] 揭棋增加首次图示引导，继续禁止可能泄露暗子身份的分析、普通棋谱和分享路径。

### 第三批：局域网大厅与房间闭环

- [x] 将“在线”统一命名为“局域网对战”，避免对公网账号能力形成错误预期。
- [x] 大厅拆分“找对局 / 创建房间”，默认先展示可加入房间；公开、最近进入、最近结束使用同一列表和筛选器。
- [x] 等待阶段使用独立准备室，不再把席位操作覆盖在棋盘上；流程统一为选择席位、准备、开始。
- [x] 邀请链接先展示房间、规则和空缺席位，确认昵称与席位后直接加入。
- [x] 悔棋、和棋、换边统一使用带倒计时的协商条；发起方可撤回，接收方明确同意或拒绝。
- [x] 房主邀请为主操作，移除成员、作废邀请、恢复链接和解散房间进入房间管理。
- [x] 保留服务端房间串行化、revision、席位授权、聊天持久化和揭棋公开投影边界。

### 第四批：跨棋类一致性、移动端与验收

- [x] 共享页面外壳、导航、阶段标签、反馈组件、房间卡片和等待室；象棋与五子棋继续隔离棋盘、状态仓库、协议和 AI 引擎。
- [x] 移动端使用棋盘优先布局和底部阶段栏，棋谱、分析、复盘、聊天进入半屏抽屉并提供未读提示。
- [x] 统一空状态、加载态、错误态、键盘操作和可访问名称。
- [x] 验收目标：老用户首页到落子不超过 3 次操作，首次用户不超过 5 次；邀请进入后两次决策内完成就座；刷新可恢复对局、席位和房间阶段。
- [x] 完成前端流程测试、客户端/服务端测试、全量构建与揭棋隐私回归。

## 已完成

| 功能                         | 状态   | 说明                                                                                                                                                           |
| ---------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 棋局工作台视觉重构           | 已完成 | 启动页与棋局页统一为“棋境”视觉体系，棋盘、记录和对局/分析/复盘/变招工具分区展示。                                                                              |
| 引擎连接状态提示             | 已完成 | 侧栏显示后端/引擎连接状态，引擎不可用时能给出原因。                                                                                                            |
| AI 思考状态提示              | 已完成 | 面板和棋盘能显示当前 AI 思考中，并禁用相关操作。                                                                                                               |
| AI 搜索超时与请求隔离        | 已完成 | 有限搜索到达 60 秒后采用当前最佳结果；前端提供最终超时保护，停止指令按会话请求匹配，断线和异常会清理等待状态。                                                 |
| FEN 导入错误详情             | 已完成 | 导入弹窗展示完整校验错误列表，并保留输入。                                                                                                                     |
| 棋谱文本复制                 | 已完成 | 当前走棋记录可复制为中文文本。                                                                                                                                 |
| 引擎 PV 中文化               | 已完成 | 分析栏展示中文记谱的推荐变化。                                                                                                                                 |
| AI 对战自动播放              | 已完成 | AI 对战支持开始/暂停和速度选择。                                                                                                                               |
| 最近导入/保存局面            | 已完成 | 支持最近 FEN 列表和手动保存。                                                                                                                                  |
| 移动端棋盘操作细节           | 已完成 | 提升触摸落点容错，支持长按取消和震动提示。                                                                                                                     |
| 分析曲线                     | 已完成 | 打开引擎分析时记录并展示红黑评估变化。                                                                                                                         |
| 残局标签体系                 | 已完成 | 自定义残局支持标签，搜索可匹配标签。                                                                                                                           |
| 残局导入/导出 JSON           | 已完成 | 自定义残局可导入导出备份。                                                                                                                                     |
| 研究局面保存                 | 已完成 | 支持保存起始 FEN、完整走法、当前步、分析曲线、标记和备注。                                                                                                     |
| 关键局面标记与备注           | 已完成 | 走棋记录支持星标和短备注，并随研究局面一起保存。                                                                                                               |
| 走法候选列表                 | 已完成 | 引擎协议支持 MultiPV，前端展示候选走法、分数、深度和中文记谱。                                                                                                 |
| 残局目标与完成判定           | 已完成 | 自定义残局可配置训练目标和目标步数，对局中显示完成/未完成反馈。                                                                                                |
| 残局标准解法保存             | 已完成 | 自定义残局可保存 UCI 标准解法，并在训练时展示进度或偏离提示。                                                                                                  |
| 研究局面导入/导出 JSON       | 已完成 | 研究局面支持 JSON 备份、迁移和导入合并。                                                                                                                       |
| 候选走法 PV 展开             | 已完成 | 候选列表展示后续 PV 中文变化线，方便比较方案。                                                                                                                 |
| 研究局面列表筛选             | 已完成 | 支持按名称、说明、走法、备注、标记和更新时间筛选研究局面。                                                                                                     |
| 残局提示分层                 | 已完成 | 基于标准解法提供方向、棋子、完整走法三层训练提示。                                                                                                             |
| 错误走法反馈增强             | 已完成 | 偏离标准解法后结合当前评估提示“仍可继续”或“明显转差”。                                                                                                         |
| 局面截图导出                 | 已完成 | 侧栏支持导出当前棋盘 PNG，便于分享局面和问题反馈。                                                                                                             |
| 服务端输入 schema 校验增强   | 已完成 | WebSocket 协议收紧 requestId、moves UCI 和候选数量校验。                                                                                                       |
| WebSocket 协议测试           | 已完成 | 覆盖消息解析、requestId、错误响应和过期响应判断。                                                                                                              |
| useGame 状态流测试           | 已完成 | 覆盖人机悔棋/重做、残局单 AI 侧、AI 自动触发条件。                                                                                                             |
| CI 工作流                    | 已完成 | GitHub Actions 执行安装、测试和构建。                                                                                                                          |
| 自然限着提醒                 | 已完成 | 连续多手未吃子时在侧栏提示风险，但不直接判和。                                                                                                                 |
| 研究局面批量管理             | 已完成 | 研究列表支持多选、批量删除、单项重命名和复制。                                                                                                                 |
| 候选走法刷新策略             | 已完成 | 候选走法支持自动刷新开关，减少每手手动点击。                                                                                                                   |
| 手动和棋与认输               | 已完成 | 双人对弈支持手动和棋；非 AI 对战模式支持当前方认输。                                                                                                           |
| 局面截图美化                 | 已完成 | 导出 PNG 叠加模式、走棋方、FEN 和局面名称，便于分享。                                                                                                          |
| 训练提示历史记录             | 已完成 | 残局训练记录本局请求过的提示层级，便于复盘。                                                                                                                   |
| 引擎搜索配置面板             | 已完成 | 候选走法支持数量设置，自动刷新支持间隔设置，提示强度可单独调整并本地保存。                                                                                     |
| 引擎深度/时间上限配置        | 已完成 | 引擎请求支持按深度或时间限制搜索成本，并复用现有设置面板持久化。                                                                                               |
| 引擎线程/Hash 配置           | 已完成 | 引擎设置支持自动/手动线程与 Hash 配置，本地持久化并通过 init 消息应用到 Pikafish。                                                                             |
| 对局回放分享链接第一版       | 已完成 | 支持复制包含起始 FEN、走法列表和当前步索引的回放链接，打开后恢复为研究回放。                                                                                   |
| 重复局面检测提醒             | 已完成 | 基于棋子布局与走棋方统计重复局面，在侧栏提醒但不直接裁定。                                                                                                     |
| 研究回放体验增强             | 已完成 | 研究模式支持自动回放、速度选择、星标跳转和标注记录筛选。                                                                                                       |
| 变招树数据模型设计           | 已完成 | 已新增 `docs/variation-tree-design.md`，覆盖节点模型、迁移策略、主线规则和分阶段落地方案。                                                                     |
| 候选变化棋盘预览             | 已完成 | MultiPV 候选支持在独立预览状态中逐手查看，不修改真实棋局。                                                                                                     |
| 研究自动保存                 | 已完成 | 已保存研究的走法、当前步、分析、星标和备注会延迟自动保存，并显示保存状态；分享回放仍需手动另存。                                                               |
| pnpm 工具链统一              | 已完成 | 固定 pnpm 9.15.9，并同步 workspace 构建脚本白名单以兼容后续版本。                                                                                              |
| 复盘错着分析                 | 已完成 | 服务端逐局面评估并反馈进度，前端按走棋方计算损失、分类疑问/错着/严重失误，可跳回走棋前局面查看推荐走法。                                                       |
| 变招树第一阶段               | 已完成 | 旧线性棋谱自动迁移；悔棋后新走法保留旧线，支持分支切换、设为主线，并随研究 JSON v2 保存。                                                                      |
| 揭棋人机模式第一阶段         | 已完成 | 除将帅外随机盖棋，支持暗子原位走法、移动翻子、私有暗吃身份、双方吃子区及官方 Pikafish `jieqi_old` 引擎。引擎按会话隔离；可能泄密的历史回退与分析工具暂不开放。 |
| 局域网房间聊天               | 已完成 | 房主、棋手和观众均可发言，最近 100 条消息独立持久化；支持限流、房主删除、成员/全员禁言及系统/房间敏感词过滤。                                                  |
| 开发环境局域网访问与分享地址 | 已完成 | `pnpm dev` 监听局域网网卡；从 localhost 创建房间时，象棋与五子棋邀请、席位恢复链接自动使用优先物理网卡 IPv4。                                                  |

## 后续需求路线图

后续主线从“补齐功能”调整为“形成研究与训练闭环”：先让用户能在局面上表达思路，再让每条变招拥有独立分析，最后把复盘结果转成可重复训练的内容。揭棋棋谱、严格裁定和公网能力分别作为独立高风险项目推进，不与普通研究数据混用。

```text
局面表达（第五批）
  -> 分支分析（第六批）
    -> 错题训练（第七批）

揭棋专用记录（第八批，独立隐私模型）
规则与互操作（第九批）
公网与账号（长期平台化）
```

### 第五批：研究标注与局面表达（已完成）

目标：让研究模式从“保存走法”升级为“能讲清楚为什么这样走”。

- [x] 增加箭头、圈点两类棋盘标注，首版提供红、绿、蓝三种颜色和一键清空当前节点标注。
- [x] 标注直接绑定 `variationTree.nodes[nodeId]`，根局面和每个变招节点分别保存；禁止使用易错位的活动线步数索引。
- [x] 桌面端支持明确的标注模式与拖拽操作；移动端通过工具栏选择“箭头 / 圈点”后触控绘制，不占用正常落子手势。
- [x] 提供标注列表、删除按钮、撤销最近一次标注和键盘可访问名称，不能只依赖颜色或画布手势。
- [x] 自动保存、复制研究、JSON 导入导出和局面截图完整保留标注；旧版 JSON v2 继续可导入，新字段缺失时按空标注处理。
- [x] 首版只开放普通象棋研究/回放，不进入实战、局域网和揭棋，避免手势冲突及暗子信息误存。
- [x] 增加节点切换、旧数据兼容、序列化和截图渲染测试，并完成桌面/移动端浏览器验收。

验收标准：切换主线或支线后只显示该节点标注；刷新、复制、导出再导入均不丢失；关闭标注模式后所有落子操作与当前版本一致。

### 第六批：变招树节点分析与分支比较（P0 / M-L）

目标：解决当前评估按活动线步数保存、切换分支后分析语义不稳定的问题。

#### 第一阶段：节点评估模型与曲线迁移（已完成）

- [x] 在变招节点保存评估值、深度、最佳着和 PV，并记录引擎配置签名与更新时间。
- [x] 活动线分析曲线从节点数据派生；切换分支时恢复该分支评估，不复用其他分支同序号的数据。
- [x] 分析请求绑定目标节点，`info` 结果按请求时的节点写回并校验走法签名，防止过期结果写入错误节点。
- [x] 旧研究首次打开按活动线索引无损迁移分析到节点；导出升级为 JSON v4，v2/v3 继续可导入。
- [x] 完成迁移、曲线派生、缓存签名与存储校验测试；揭棋仍不出现普通分析入口。

#### 第二阶段：分支分析与比较（已完成）

- [x] 支持分析当前节点、分析当前分支和停止任务；已完成且配置一致的节点默认复用缓存（跳过重复请求）。
- [x] 增加双分支比较视图，展示关键节点胜率/分值差、推荐变化和主要分歧，不自动改写主线。
- [x] 删除分支时同步清理该分支评估；设为主线、复制研究、导入导出时保持节点身份和评估归属。
- [x] 复盘结果改为引用 `nodeId`；引擎请求继续保持 requestId、历史签名和会话隔离，防止过期结果写入错误节点。

验收标准：同一深度分析两条分支后反复切换，曲线与推荐着稳定对应各自节点；旧研究首次打开可无损迁移；揭棋仍不出现普通分析入口。

### 第七批：复盘到训练的闭环（P1 / M）

目标：让“发现错着”最终转化为“以后会做”，而不是停留在一次性报告。

- [ ] 在错着/严重失误卡片提供“加入训练”操作，保存走棋前局面、实战着、推荐着、来源对局和节点引用。
- [ ] 新增训练队列，支持按未练、待复习、已掌握筛选，并记录尝试次数、最近结果和最近练习时间。
- [ ] 训练时先让用户独立走棋，再分层展示方向提示、候选棋子和推荐变化；不默认显示引擎答案。
- [ ] 以“走出推荐着或评估损失在阈值内”为通过条件；引擎不可用时退化为推荐着匹配，不阻塞本地训练。
- [ ] 支持从训练题回到来源研究节点，查看原分支、备注、标注和评估。
- [ ] 提供批量删除、JSON 备份和去重；同一研究节点重复加入时更新来源，不制造重复题目。
- [ ] 完成训练判定、持久化、去重、无引擎降级和移动端流程测试。

验收标准：用户从复盘错着两次操作内加入训练；刷新后可继续；完成一次训练后状态立即更新，并可追溯到原局面。

### 第八批：揭棋专用棋谱与隐私复盘（P1 / L）

目标：在不泄露暗子身份的前提下，为揭棋建立独立保存和回放能力。

- [ ] 先产出 `JieqiRecord` 数据设计：公开事件流、本人可见信息、完整裁判信息三层分离，禁止复用普通 FEN/回放链接。
- [ ] 第一阶段只支持本机揭棋对局保存与本人视角回放；明确何时允许展示全部身份，并在 UI 中标注当前视角。
- [ ] 第二阶段再支持局域网记录：服务端按席位生成不同投影，观众、红方、黑方的持久化内容不得越权。
- [ ] 分享功能默认只输出公开投影；任何包含私有身份的导出必须二次确认并使用不同文件类型和视觉警示。
- [ ] 普通棋谱、分析曲线、变招树和训练题不得通过兼容逻辑意外读取揭棋私有数据。
- [ ] 建立跨视角快照测试、暗吃身份测试、断线恢复测试和导出内容扫描，作为发布门禁。

验收标准：用红、黑、观众三个身份回放同一局，任一时间点只能看到当时有权获知的信息；普通象棋导入导出行为不受影响。

### 第九批：规则质量与棋谱互操作（P2 / L）

- [ ] 严格长将/长捉裁定：先形成可执行规则案例集，再实现责任方识别、一将一捉和多循环边界；不能直接替换现有简化裁定。
- [ ] 棋谱格式导入导出：先支持一种有明确规范、可稳定测试的象棋格式，再评估 CBR/XQF 等兼容成本。
- [ ] 开局库：先确认数据来源、许可和版本更新策略，再接入名称识别、局面命中和研究入口。
- [ ] 回放分享增强：完整变招树分享、封面和权限依赖服务端短链时，再单独设计存储与失效策略。

验收标准：每项能力都必须有外部规范或固定测试语料；规则判断和格式转换不得仅依赖 UI 冒烟测试。

## 长期平台化方向（P2，暂不排期）

| 方向               | 前置条件                                    | 当前决策                                         |
| ------------------ | ------------------------------------------- | ------------------------------------------------ |
| 公网对战           | TLS、部署拓扑、限流、防滥用、身份与断线策略 | 继续保留“局域网对战”命名，不提前包装成公网产品。 |
| 账号与云端收藏     | 账号体系、数据归属、删除/导出、冲突合并     | 本地研究与训练闭环稳定后再评估。                 |
| 在线协作研究       | 云端变招树、并发编辑、成员权限、审计        | 不与第六批本地节点分析混做。                     |
| 云端短链与分享权限 | 服务端存储、过期机制、私密访问与滥用治理    | 普通回放先维持本地编码链接。                     |
| 主题与视觉个性化   | 设计令牌、棋盘素材规范、导出一致性          | 收益低于研究与训练主线，暂缓。                   |

## 跨批次产品与技术边界

- 普通象棋、揭棋、五子棋继续隔离规则、状态仓库、协议和 AI 引擎；只共享无棋类语义的页面与反馈组件。
- 新持久化字段必须向后兼容，读取旧数据时迁移，写入失败时保留可重试快照；不做破坏式一次性升级。
- 研究节点是后续标注、评估、复盘和训练来源的统一身份，新增能力优先引用 `nodeId`，不继续扩大步数索引债务。
- 揭棋任何新路径先定义公开/席位私有/裁判完整三个投影，再实现 UI；隐私测试失败即阻止发布。
- 所有移动端新增操作必须保持棋盘优先，不得制造页面横向滚动；核心按钮触控区域不小于 44px。
- 每批完成前固定执行客户端/服务端测试、全量构建、`git diff --check`、桌面/移动端流程验收和浏览器控制台检查。

## 推荐推进顺序

1. **第六批：变招树节点分析与分支比较**——消除现有步数索引限制，建立统一节点数据底座。
2. **第七批：复盘到训练的闭环**——把已有复盘、研究和残局能力连接成可重复使用的产品流程。
3. **第八批：揭棋专用棋谱与隐私复盘**——独立立项，以隐私模型和测试门禁先行。
4. **第九批：规则质量与棋谱互操作**——按规范与测试语料逐项推进，不与主线体验改造并行扩张。

## 当前建议启动范围

- 第六批（节点级评估、显式节点/分支分析、缓存复用、双分支比较与 `nodeId` 复盘引用）已完成。
- 下一步建议：`P1/M` 第七批——先落地“从错着/严重失误加入训练”与训练题存储、去重，再补训练队列和练习流程。
- 训练题继续引用研究 `nodeId`；无引擎时按推荐着匹配降级，不能阻塞本地训练。

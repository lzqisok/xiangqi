# 待办清单

本文档按当前 `main` 分支功能现状维护。目标不是罗列所有想法，而是把「现在最值得做、做完能明显提升可用性」的任务排清楚。

优先级含义：

- `P0`：当前最实用、建议下一批优先做。
- `P1`：价值明确，但需要先设计数据模型或与 P0 打配合。
- `P2`：长期产品化方向，投入较大，暂不作为近期主线。

难度：

- `S`：小改动，通常 0.5 天内。
- `M`：中等功能，通常 1-2 天。
- `L`：较大功能或跨模块改造，需要拆阶段。

## 已完成

| 功能 | 状态 | 说明 |
| --- | --- | --- |
| 引擎连接状态提示 | 已完成 | 侧栏显示后端/引擎连接状态，引擎不可用时能给出原因。 |
| AI 思考状态提示 | 已完成 | 面板和棋盘能显示当前 AI 思考中，并禁用相关操作。 |
| FEN 导入错误详情 | 已完成 | 导入弹窗展示完整校验错误列表，并保留输入。 |
| 棋谱文本复制 | 已完成 | 当前走棋记录可复制为中文文本。 |
| 引擎 PV 中文化 | 已完成 | 分析栏展示中文记谱的推荐变化。 |
| AI 对战自动播放 | 已完成 | AI 对战支持开始/暂停和速度选择。 |
| 最近导入/保存局面 | 已完成 | 支持最近 FEN 列表和手动保存。 |
| 移动端棋盘操作细节 | 已完成 | 提升触摸落点容错，支持长按取消和震动提示。 |
| 分析曲线 | 已完成 | 打开引擎分析时记录并展示红黑评估变化。 |
| 残局标签体系 | 已完成 | 自定义残局支持标签，搜索可匹配标签。 |
| 残局导入/导出 JSON | 已完成 | 自定义残局可导入导出备份。 |
| 研究局面保存 | 已完成 | 支持保存起始 FEN、完整走法、当前步、分析曲线、标记和备注。 |
| 关键局面标记与备注 | 已完成 | 走棋记录支持星标和短备注，并随研究局面一起保存。 |
| 走法候选列表 | 已完成 | 引擎协议支持 MultiPV，前端展示候选走法、分数、深度和中文记谱。 |
| 残局目标与完成判定 | 已完成 | 自定义残局可配置训练目标和目标步数，对局中显示完成/未完成反馈。 |
| 残局标准解法保存 | 已完成 | 自定义残局可保存 UCI 标准解法，并在训练时展示进度或偏离提示。 |
| 研究局面导入/导出 JSON | 已完成 | 研究局面支持 JSON 备份、迁移和导入合并。 |
| 候选走法 PV 展开 | 已完成 | 候选列表展示后续 PV 中文变化线，方便比较方案。 |
| 研究局面列表筛选 | 已完成 | 支持按名称、说明、走法、备注、标记和更新时间筛选研究局面。 |
| 残局提示分层 | 已完成 | 基于标准解法提供方向、棋子、完整走法三层训练提示。 |
| 错误走法反馈增强 | 已完成 | 偏离标准解法后结合当前评估提示“仍可继续”或“明显转差”。 |
| 局面截图导出 | 已完成 | 侧栏支持导出当前棋盘 PNG，便于分享局面和问题反馈。 |
| 服务端输入 schema 校验增强 | 已完成 | WebSocket 协议收紧 requestId、moves UCI 和候选数量校验。 |
| WebSocket 协议测试 | 已完成 | 覆盖消息解析、requestId、错误响应和过期响应判断。 |
| useGame 状态流测试 | 已完成 | 覆盖人机悔棋/重做、残局单 AI 侧、AI 自动触发条件。 |
| CI 工作流 | 已完成 | GitHub Actions 执行安装、测试和构建。 |
| 自然限着提醒 | 已完成 | 连续多手未吃子时在侧栏提示风险，但不直接判和。 |
| 研究局面批量管理 | 已完成 | 研究列表支持多选、批量删除、单项重命名和复制。 |
| 候选走法刷新策略 | 已完成 | 候选走法支持自动刷新开关，减少每手手动点击。 |
| 手动和棋与认输 | 已完成 | 双人对弈支持手动和棋；非 AI 对战模式支持当前方认输。 |
| 局面截图美化 | 已完成 | 导出 PNG 叠加模式、走棋方、FEN 和局面名称，便于分享。 |
| 训练提示历史记录 | 已完成 | 残局训练记录本局请求过的提示层级，便于复盘。 |
| 引擎搜索配置面板 | 已完成 | 候选走法支持数量设置，自动刷新支持间隔设置，提示强度可单独调整并本地保存。 |
| 引擎深度/时间上限配置 | 已完成 | 引擎请求支持按深度或时间限制搜索成本，并复用现有设置面板持久化。 |
| 引擎线程/Hash 配置 | 已完成 | 引擎设置支持自动/手动线程与 Hash 配置，本地持久化并通过 init 消息应用到 Pikafish。 |
| 对局回放分享链接第一版 | 已完成 | 支持复制包含起始 FEN、走法列表和当前步索引的回放链接，打开后恢复为研究回放。 |
| 重复局面检测提醒 | 已完成 | 基于棋子布局与走棋方统计重复局面，在侧栏提醒但不直接裁定。 |
| 研究回放体验增强 | 已完成 | 研究模式支持自动回放、速度选择、星标跳转和标注记录筛选。 |
| 变招树数据模型设计 | 已完成 | 已新增 `docs/variation-tree-design.md`，覆盖节点模型、迁移策略、主线规则和分阶段落地方案。 |
| 候选变化棋盘预览 | 已完成 | MultiPV 候选支持在独立预览状态中逐手查看，不修改真实棋局。 |
| 研究自动保存 | 已完成 | 已保存研究的走法、当前步、分析、星标和备注会延迟自动保存，并显示保存状态；分享回放仍需手动另存。 |
| pnpm 工具链统一 | 已完成 | 固定 pnpm 9.15.9，并同步 workspace 构建脚本白名单以兼容后续版本。 |

## P0：下一批最实用

| 项目 | 难度 | 实用性 | 建议说明 |
| --- | --- | --- | --- |
| 复盘错着分析 | L | 高 | 对比实战走法前后的稳定评估，标记疑问、错着和严重失误，并支持跳回对应局面查看推荐变化。 |

## P1：适合第二阶段做

| 项目 | 难度 | 实用性 | 建议说明 |
| --- | --- | --- | --- |
| 变招树第一阶段 | L | 高 | 在数据模型设计完成后实施：悔棋后走新棋不覆盖旧线，保留主线/支线，并支持基础分支切换。 |
| 棋盘箭头与圈点标注 | M | 中高 | 在研究模式中增加箭头、圈点等局面标注，并随研究数据保存。 |

## P2：大功能，暂缓

| 项目 | 难度 | 实用性 | 建议说明 |
| --- | --- | --- | --- |
| 长将/长捉/重复局面规则 | L | 中高 | 中国象棋规则边界复杂，容易引入争议，建议先做局面重复检测和测试用例，再考虑严格裁定。 |
| 在线房间对战 | L | 高 | 房间号、红黑选边、观战、断线重连；需要新的服务端状态模型。 |
| 对局回放分享增强 | L | 中 | 在第一版分享链接稳定后，再考虑云端短链、封面图、权限控制或与变招树结合。 |
| 账号与云端收藏 | L | 中 | 需要后端存储和账号体系，暂不适合当前阶段。 |
| 开局库 | L | 中 | 需要数据来源和维护策略，暂不作为近期主线。 |
| 棋谱文件格式支持 | L | 中 | 支持 PGN/CBR/XQF 等格式导入导出，优先级低于内部 JSON。 |
| 棋盘主题切换 | M | 中 | 可做，但当前收益低于研究/训练闭环。 |

## 推荐推进顺序

1. **复盘错着分析**：复用分析曲线、候选变化和历史跳转，形成从对局到复盘的学习闭环。
2. **变招树第一阶段**：基于 `docs/variation-tree-design.md` 实施线性迁移、分支保留和基础切换。
3. **棋盘箭头与圈点标注**：增强研究表达，并为后续变招树节点标注打基础。

## 当前最建议的一批任务

- `P0/L` 复盘错着分析

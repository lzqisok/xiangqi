# A 阶段本地验证记录（2026-09-07）

基于 `codex/online-mysql-foundation` 工作区开发；保留用户已有的 TODO 重排。本报告记录提交前的代码与本地验收结果。测试参数和现场报告命令见 [验收说明](../online-reliability-acceptance.md)。

| 检查                | 执行结果                            | 证据范围                                                                                            |
| ------------------- | ----------------------------------- | --------------------------------------------------------------------------------------------------- |
| 服务端回归          | 151/151 通过，0 skip                | 包含 8 个真实 MySQL 集成用例                                                                        |
| 最终 MySQL 定向重跑 | 8/8 通过                            | 积分触发器故障回滚、无客户端操作时后台重试、超时/落子/掉线竞态、过期 presence 写入、恢复 CLI 退出码 |
| 客户端回归          | 182/182 通过                        | 原有玩法与共享契约回归                                                                              |
| 验收工具回归        | HTTP/WS/TCP/应用子进程定向测试      | 401/403/429、未认证语义、业务负载全流程、失败钩子阻断、代理故障与应用重启                           |
| 构建与契约          | `pnpm build`、`pnpm contract:check` | TypeScript、Vite 与 API/WS 契约标记                                                                 |

真实 MySQL 使用项目已有 `TEST_DATABASE_URL`，由集成测试创建随机隔离库，并在 finally 删除。没有修改开发业务库。测试中故意让 rating_ledger 插入失败，确认对局阶段与 revision 未提交；解除故障后仅后台任务访问业务方法即完成一次结算。另以原截止时间之后写入离线状态，验证不再违反时间顺序约束。

恢复校验用例依次破坏积分余额、公开状态白名单和 match_states 存在性，确认检查拒绝；积分余额破坏时另启动真实 restore CLI，确认退出码为 1，且不输出数据载荷。HTTP 夹具确认读负载在错误状态/语义下非零退出，业务夹具覆盖登录、两个并发匹配请求、第三方观战、聊天、四手交替走子、终局、双方历史与退出。

本机隔离故障适配器测试使用临时 TCP 服务和自己创建的子进程，不操作实际部署进程。它验证故障注入、恢复和失败阻断的工具行为，不代表 MySQL/应用部署全链路四场景演练已完成。

未执行：真实 staging 容量阶梯压测、真实备份导入空库恢复演练、实际部署应用重启/故障全链路、告警送达、生产容量判定。这些继续留在 C 阶段及原批次环境门禁，不能用本报告代替。默认容量目标为 5 盘/15 账号、每档 10 轮、操作 p95≤1000ms、错误率与限流率为零，尚无实测生产容量结论。

# B 阶段完成报告

日期：2026-09-11。分支 `codex/online-mysql-foundation`，基于 `37b34c4` 和前两子批未提交改动继续完成。B 的代码与本地验证完成，不代表公网开放门禁完成。

## 完成范围

- 单方断线正常计分，开局后零走子同样受规则约束；双方宽限均到期不计分，排位禁止申请/接受悔棋。
- 同分池同对手 24 小时 3 局上限，持久化取消计数/冷却及固定标签指标，唯一队列与并发配对/取消约束。
- 两类引擎 WebSocket 在请求和结果发送时按数据库活跃排位状态校验账号；覆盖其他连接与设备、先分析后开局，查询失败拒绝输出。本地/LAN 玩法保持独立。
- 本人每局积分查询、未计分原因、原结算作废展示与补偿流水、分页加载/重试、账号切换隔离。没有客户端写分或指定查询其他所有者的入口。
- 进程启动中止尚未结束的排位并记录 service_restart；数据库不可用造成自动裁决失败时，恢复优先中止并记录 service_failure。人工 admin_abort 使用本机 CLI，审计保存操作者和原因，与正常终局互斥，无普通用户路由。

## 验证结果

- 客户端 183/183 通过，包含新增积分/作废/未计分文案用例。
- 服务端 159/159 通过，包含 11 项真实 MySQL 集成测试，无跳过。
- 全量构建、Prettier、契约标记及 diff 空白检查通过。
- 真 MySQL：断线结算失败回滚与并发去重、取消窗口/冷却与配对竞态、积分时间相同的游标分页、跨账号/游标拒绝、原结算与补偿、无效参与者不结算、重启/管理员/数据库故障中止、审计、重复中止以及中止/超时竞态。
- HTTP：实际 Express 路由验证本人详情 200、其他账号 404、伪造 owner 查询不串读、错误分页 400、普通中止路由不存在。测试注入认证 actor，认证底座由现有账号测试覆盖。
- WebSocket：实际五子棋连接验证活跃排位拒绝请求，以及引擎计算途中进入排位后不发送结果；共用授权函数验证状态重新查询与数据库失败拒绝。普通象棋/揭棋入口和输出均接入该函数，但本批没有以真实 Pikafish 运行完整跨设备 E2E。
- 浏览器：挂载实际 AuthProvider/积分组件的临时夹具，模拟接口响应，验证首批 20 条、下一页失败/重试后追加第 21 条、末页停止分页、账号切换移除原账号流水并展示空状态；控制台无新增错误，390×844 移动布局无横向溢出。该检查是组件级验证，不是实际账号登录到对局的 staging E2E。夹具与临时 Vite 服务已清理。

使用本任务临时 MySQL 9.4 数据目录和回环 13316 端口，测试随机创建/清理数据库，没有修改开发业务库。

## 发布边界

当前最高 migration 为 0009，发布前先迁移，再启动应用。启动会中止仍在进行的排位且不计分，发布前应摘流并等待排位结束。旧应用的精确 schema 校验不兼容新 schema，不能直接作为已验证的热回滚包。

无法凭本地检测判断全部代理或网络故障，运维仍需现场证据；客户端不能自报故障免扣分。无法阻止外部引擎、外部设备或另一个账号作弊。

C 阶段 staging、备份恢复、告警送达、容量调优及发布回滚演练未执行；D 阶段完整管理后台、分差匹配、历史分页、导出和数据生命周期继续待办。代码未提交、未推送。

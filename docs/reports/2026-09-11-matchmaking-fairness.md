# B 第二子批：匹配公平性回归

日期：2026-09-11，分支 `codex/online-mysql-foundation`。在上一子批未提交改动上继续开发。

## 实现

同分池同对手最近 24 小时最多开局 3 次排位，涵盖进行中对局及不同棋钟；取消计数窗口与冷却写入新增数据库表，跨会话及服务重建仍有效。配对和取消共用容量锁，保留账号唯一队列。补充固定标签指标、客户端规则及冷却错误提示。

## 验证

- 客户端 182/182、服务端 155/155 通过；包含 10 项真实 MySQL 集成测试，无跳过。
- 全量构建、格式检查、契约标记检查和 `git diff --check` 通过。
- 新增回归验证跨棋钟累计、达到上限转为等待、第三方可配对、休闲/分池隔离、24 小时到期、多会话切换条件保持唯一队列、重复取消只计一次、冷却拒绝与到期恢复、请求重放及取消/配对竞态。
- 临时 MySQL 9.4 仅绑定回环 13316 端口，测试创建随机隔离数据库并清理；未修改开发业务库。验证了 schema 8 空库迁移和旧版顺序升级。

## 边界

未执行浏览器桌面/移动端全流程、真实 staging 容量调优或回滚演练。代码尚未提交推送。

本批不增加外部引擎识别、多账号串通识别、跨连接辅助限制或积分流水 UI；后两项继续按 TODO 推进。历史数据清理不得删除防刷分窗口内所需事实。上线前需迁移 schema 8，旧 schema 7 应用不能直接作为已验证回滚包。

# B 第一子批：断线计分与排位禁悔棋

日期：2026-09-11。基线：`37b34c4`，分支 `codex/online-mysql-foundation`。

## 交付范围

- 已开局排位单方断线判负纳入积分资格，终局、分数和流水共用事务；未开局不计分，双方宽限均到期按放弃、不计分。
- 三棋类排位禁止申请和接受悔棋；客户端禁用相关按钮并展示公平竞赛规则。
- 新增资格、三棋类协商及真实 MySQL 事务回滚和并发裁决用例。

## 验证

- 客户端：182/182 通过。
- 服务端：154/154 通过，包含 9 项真实 MySQL 集成测试；无跳过。
- `pnpm build`、`pnpm format:check`、`pnpm contract:check`、`git diff --check` 通过。
- 原本机 3306 未监听。使用本机 MySQL 9.4 的临时独立数据目录、仅回环地址 13316 端口，测试随机创建并清理数据库，没有修改开发业务库。
- 新增数据库用例故意使 `rating_ledger` 插入失败，验证阶段和 revision 回滚且积分不存在；解除故障后并发裁决仅成功一次。零走子败方从 1500 降为 1480，胜方为 1520；随后双方放弃不增加计分局数，恢复一致性检查通过。

## 未完成边界

没有执行浏览器桌面/移动端全流程或 staging 验收。未提交或推送。

B 阶段仍待服务故障识别与管理员中止流程、防刷分、取消滥用、账号跨连接辅助限制及分页积分流水；本批不重算历史断线结算，也不表示已满足公网排位开放门禁。
